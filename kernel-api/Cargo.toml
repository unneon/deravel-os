[package]
name = "deravel-kernel-api"
version = "0.0.0"
edition = "2024"

[dependencies]
deravel-types = { path = "../types" }
log = "0.4"
postcard = "1.1"
riscv = "0.16"
serde = { version = "1.0", features = ["alloc", "unstable"], default-features = false }

[build-dependencies]
deravel-codegen = { path = "../codegen" }
