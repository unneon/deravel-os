[workspace]
members = [
    "apps",
    "codegen",
    "filesystems/fat",
    "filesystems/tar",
    "kernel",
    "kernel-api",
    "types",
]
default-members = ["kernel"]
resolver = "3"

[profile.dev]
opt-level = 1
